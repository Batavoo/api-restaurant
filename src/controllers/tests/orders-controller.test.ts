import { AppError } from '@/utils/AppError';
import { OrdersController } from '../orders-controller';

describe('OrdersController', () => {
  it('should throw error when session table is not found', () => {});
});
